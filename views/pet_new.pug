extends layout 

block content
  h1 Create new pet
  <br>
  form(method="POST")
    div.form-group
      label(for='pet_name') Pet Name:
      input#pet_name.form-control(type='text', placeholder='Doggo' name='name' required)
    div(class='update-controls')
      button.btn.btn-primary(type='submit') Submit